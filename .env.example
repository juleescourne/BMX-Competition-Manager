# Optional. If omitted, the app generates a temporary development secret at startup.
SECRET_KEY=replace-with-a-long-random-value

# Optional. Defaults to a local SQLite database in instance/bmx.db.
# Examples:
# DATABASE_URL=sqlite:////absolute/path/to/bmx.db
# DATABASE_URL=postgresql://user:password@localhost/bmx
